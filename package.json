{
  "name": "refund-claim-integrity",
  "version": "0.1.0",
  "private": true,
  "description": "Refund Claim Integrity Engine (RCIE) — defense-only refund claim verification. Razorpay AI Buildathon, Track 02.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "demo:spine": "node --env-file-if-exists=.env scripts/demo-spine.ts",
    "demo:decide": "node --env-file-if-exists=.env scripts/demo-decide.ts",
    "demo:full": "node --env-file-if-exists=.env scripts/demo-full.ts",
    "eval": "node --env-file-if-exists=.env eval/report.ts",
    "dashboard": "node --env-file-if-exists=.env dashboard/generate.ts",
    "typecheck": "tsc --noEmit",
    "fetch:fraudbench": "node --env-file-if-exists=.env eval/fraudbench/fetch.ts",
    "eval:live": "node --env-file-if-exists=.env eval/live_batch.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "jpeg-js": "^0.4.4"
  }
}
